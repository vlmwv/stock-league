[variables]
    NIXPACKS_NODE_VERSION = "22"
    NIXPACKS_NO_CI = "1"

[phases.setup]
    nixPkgs = ["nodejs_22", "python3", "pkg-config"]

[phases.install]
    dependsOn = ["setup"]
    cmds = ["rm -f package-lock.json", "npm install --legacy-peer-deps"]
